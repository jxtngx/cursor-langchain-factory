---
description: No API keys in git. Fake model in default tests.
globs:
  - "**/*"
alwaysApply: true
---

# No secrets

- Never commit `.env` or LangSmith / provider keys.
- Default tests use a fake chat model.
- If a key appears in a diff, stop and tell the user to rotate. Do not echo it.
