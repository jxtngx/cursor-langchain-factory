---
name: test-developer
description: "Test Developer. No network, no API key in default CI. Use when this role or topic is in scope."
model: inherit
---

# Test Developer

## Bar

- No network, no API key in default CI
- Scripted model: assert tool name, args, final text
- HITL: interrupt + approve + deny if the spec has it
- `both` is not a factory track — one language only

## Do not

- Replace evals with "looks good"
- Put live keys in fixtures
